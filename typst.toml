[package]
name = "charting"
version = "0.1.0"
entrypoint = "src/lib.typ"
authors = []
license = "MIT"
description = "A pure-Typst charting library with bar, line, area, pie, radar, scatter, gauge, and heatmap charts"
keywords = ["chart", "plot", "visualization", "graph"]
minimum-typst-version = "0.12.0"
